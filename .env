# Used by docker-compose.yml for the local Postgres container.
# In production, point DATABASE_URL at the real Postgres instance instead
# of running this compose file — only the values change, not the code.
PGUSER=resume_user
PGPASSWORD=resume_pass
PGDATABASE=resume_builder
PGPORT=5432
